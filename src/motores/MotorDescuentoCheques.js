function fecha(v){if(!v)return null;const d=new Date(`${v}T00:00:00`);return Number.isNaN(d.getTime())?null:d;}
export function calcularDias(fechaOperacion,fechaVencimiento){const a=fecha(fechaOperacion),b=fecha(fechaVencimiento);if(!a||!b)return 0;return Math.max(0,Math.round((b-a)/86400000));}
export function calcularCheque(cheque,fechaOperacion,tasaDescuentoAnual,diasBase=360){const monto=Number(cheque?.monto)||0,tasa=Number(tasaDescuentoAnual)||0,dias=calcularDias(fechaOperacion,cheque?.vencimiento),interes=monto*tasa*(dias/diasBase),neto=Math.max(0,monto-interes);return {...cheque,monto,dias,interes,neto};}
export function calcularOperacion({fechaOperacion,cheques=[],tasaDescuento=0,comision=0,iva=.10,gastos=0,diasBase=360}){const detalles=cheques.map(c=>calcularCheque(c,fechaOperacion,tasaDescuento,diasBase));const importeBruto=detalles.reduce((s,c)=>s+c.monto,0),intereses=detalles.reduce((s,c)=>s+c.interes,0),ivaIntereses=intereses*iva,comisionReal=Number(comision)||0,ivaComision=comisionReal*iva,gastosReal=Number(gastos)||0,costoTotal=intereses+ivaIntereses+comisionReal+ivaComision+gastosReal,neto=Math.max(0,importeBruto-costoTotal),plazoPromedio=importeBruto?detalles.reduce((s,c)=>s+c.monto*c.dias,0)/importeBruto:0,costoPct=importeBruto?costoTotal/importeBruto*100:0;return {detalles,importeBruto,intereses,ivaIntereses,comision:comisionReal,ivaComision,gastos:gastosReal,costoTotal,neto,plazoPromedio,costoPct};}
export function crearChequeVacio(){return {numero:"",banco:"",monto:0,vencimiento:""};}